services:
  postgres:
    image: postgres:15-alpine
    container_name: testing_playground_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: testing_playground_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d testing_playground_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
